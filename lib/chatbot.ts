export interface ChatMessage { role: "user" | "assistant"; content: string; timestamp: Date; } export interface ChatResponse { response: string; error?: string; } export interface LearningContext { courseTitle?: string; lessonTitle?: string; currentTopic?: string; userProgress?: string; } export class LearningChatbot { private conversationHistory: ChatMessage[] = []; private context: LearningContext = {}; constructor(context?: LearningContext) { if (context) { this.context = context; } } public async sendMessage(message: string): Promise<ChatResponse> { const lowerMessage = message.toLowerCase(); let response = ""; if (lowerMessage.includes("c++") || lowerMessage.includes("cpp")) { response = "**C++** is a powerful programming language used for system software, game development, and high-performance applications. It combines high-level and low-level programming features. Key features include object-oriented programming, memory management, and a rich standard library. Would you like to learn about specific C++ concepts like variables, functions, or classes?"; } else if (lowerMessage.includes("variable")) { response = "A **variable** is a container that stores data in programming. In C++: `int age = 25; string name = \"John\";` Variables have names, can store different data types, and values can be changed during program execution."; } else if (lowerMessage.includes("function")) { response = "A **function** is a reusable block of code. In C++: `int add(int a, int b) { return a + b; }` Functions help organize code, make it reusable, and easier to maintain."; } else if (lowerMessage.includes("hello") || lowerMessage.includes("hi")) { response = "Hello! 👋 I am your AI learning assistant. I can help explain programming concepts, provide study tips, and answer your questions about C++ and other topics. What would you like to learn about?"; } else { response = "I am here to help you learn! I can explain programming concepts, provide study tips, and answer questions about C++. What specific topic would you like to know more about?"; } const userMessage: ChatMessage = { role: "user", content: message, timestamp: new Date() }; const assistantMessage: ChatMessage = { role: "assistant", content: response, timestamp: new Date() }; this.conversationHistory.push(userMessage, assistantMessage); return { response }; } public async getStudyTips(topic?: string): Promise<string> { return "Here are 5 study tips: 1. Practice coding daily 2. Build small projects 3. Read others code 4. Use active recall 5. Take regular breaks"; } public async explainConcept(concept: string): Promise<string> { return `I can explain ${concept}! What specific aspect would you like to know about?`; } public async getMotivationalMessage(): Promise<string> { return "You are doing great! Every expert was once a beginner. Keep practicing and you will improve! 💪"; } public getConversationHistory(): ChatMessage[] { return [...this.conversationHistory]; } public updateContext(newContext: Partial<LearningContext>): void { this.context = { ...this.context, ...newContext }; } public clearHistory(): void { this.conversationHistory = []; } }
